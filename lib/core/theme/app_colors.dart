import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Surfaces — koyu lounge teması
  static const Color background = Color(0xFF0B0B0E);
  static const Color surface = Color(0xFF161620);
  static const Color surfaceElevated = Color(0xFF1F1F2A);

  // Text
  static const Color textPrimary = Color(0xFFF5F5F7);
  static const Color textSecondary = Color(0xFFA1A1AA);
  static const Color textTertiary = Color(0xFF6B6B73);

  // Stroke / divider
  static const Color hairline = Color(0xFF24242E);

  // Default accent (mood'a göre overridable)
  static const Color accentDefault = Color(0xFFE9B040);

  static const Color shadow = Color(0x40000000);
}
